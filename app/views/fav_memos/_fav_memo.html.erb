<% if memo.liked_by?(current_user) %>
  <p><%=link_to "★", memo_fav_memos_path(memo.id), method: :delete, remote: true %></p>
<% else %>
  <p><%=link_to "☆", memo_fav_memos_path(memo.id), method: :post, remote: true %></p>
<% end %>